{
  "name": "excalidash",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "install:all": "npm --prefix backend ci && npm --prefix frontend ci && npm --prefix e2e ci",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix frontend test && npm --prefix backend test",
    "test:e2e": "npm --prefix e2e test",
    "lint": "eslint backend/src backend/scripts e2e scripts && npm --prefix frontend run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:env": "node scripts/check-env-boundary.cjs",
    "check": "npm run format:check && npm run lint && npm run check:env"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "prettier": "^3.9.0",
    "typescript-eslint": "^8.46.3"
  }
}
